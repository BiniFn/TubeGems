services:
  - type: web
    name: tubegems
    env: python
    buildCommand: npm install && npm run build && pip install -r requirements.txt
    startCommand: gunicorn main:app
    preDeployCommand: echo "No pre-deploy command needed"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
